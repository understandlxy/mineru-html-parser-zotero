# Vendor Notes

This directory contains a bundled KaTeX build used to render MinerU LaTeX fragments as MathML inside generated HTML attachments.

The KaTeX integration was added after studying the rendering approach used by the Full Text Translate Zotero plugin.
